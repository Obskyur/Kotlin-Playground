fun main() {
    val x: Int = 20
    val y: Int = 30
    val sum: Int = x + y
    val diff: Int = x - y
    val product: Int = x * y
    val quotient: Float = x.toFloat() / y
    val lt: Boolean = x < y
    val gt: Boolean = x > y
    val lte: Boolean = x <= y
    val gte: Boolean = x >= y
    val eq: Boolean = x == y
    val neq: Boolean = x != y
    println(quotient)
}